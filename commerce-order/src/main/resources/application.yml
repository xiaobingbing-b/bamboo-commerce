spring:
  application:
    name: commerce-member
  datasource:
    url: jdbc:mysql://localhost:3306/commerce_member?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: tangbing
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 8003